// Where the backend API lives. Defaults to "../mobile-api" which is
// correct when this app is deployed as a sibling folder of mobile-api/
// on the same domain (e.g. https://pkluxury.example.com/mobile-app/ next
// to https://pkluxury.example.com/mobile-api/) — the setup this README
// walks through. If you deploy the PWA on its own domain instead, change
// this to the full URL, e.g. "https://api.pkluxury.example.com/mobile-api".
const API_BASE_URL = '../mobile-api';
